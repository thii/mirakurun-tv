// Shared signing settings for local development.
// Local team credentials should be placed in Signing.local.xcconfig (gitignored).

CODE_SIGN_STYLE = Automatic
DEVELOPMENT_TEAM =

#include? "Signing.local.xcconfig"
